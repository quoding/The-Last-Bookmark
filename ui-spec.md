# 마지막 책갈피 — UI 사양서

> v2.0 · 2026-09-05
> 기준: `docs/story.md` v0.3
> 이 문서는 화면 구성과 동작을 정한다. 세계관·캐릭터·서사 규칙은 story.md, 기술 구현은 CLAUDE.md를 따른다.

## 1. 적용 범위

사이책방의 마지막 영업일 20:30~21:00을 다룬다. 실제 플레이 목표는 10~15분, 4장면 12턴이다.

**만들 화면:** 시작 / 외형 선택 / 대화 / 엔딩 / 회차 목록
**만들지 않는 화면:** 시나리오 선택, 계정 설정, 캐릭터 성격 편집

**PC 우선.** 모바일은 레이아웃이 깨지지 않는 수준만 대응하고 최적화하지 않는다. 심사자는 PC로 열 것을 전제한다.

| 이전 전제 | 이번 사양 |
|---|---|
| 여러 날에 걸친 진행 | 하루 저녁, 이야기 속 30분 |
| 날짜별 구분선 | 시각과 이름을 붙인 장면 구분선 |
| 모바일 우선 단일 열 | PC 우선 좌우 2단 |
| 인물 없는 가로 삽화 | 서윤 입상 세로 이미지 |
| 사전 제작 이미지 커밋 | 회차마다 6장 생성 |
| 고정 외형 | 플레이어가 프리셋으로 선택 |

## 2. 공통 레이아웃

### 2.1 기본 골격

- 전체 최대 폭 **1440px**, 중앙 정렬
- 좌측 이미지 컬럼 **고정 480px** (이미지 2:3 → 높이 720px)
- 우측 대화 컬럼이 나머지를 채운다
- 창 폭 1200px 미만에서 이미지 컬럼이 400px까지 축소
- 창 폭 900px 미만에서 세로 스택으로 전환 (이미지 상단, 대화 하단)

### 2.2 톤

성인 대상 잔잔한 로맨스. 종이·책·따뜻한 조명의 질감. 채도 낮은 색, 부드러운 곡률. 게임 UI지만 화려한 이펙트나 게이지 바 없음. 읽기 중심.

한국어 전용. 모든 시각은 24시간제 `HH:mm`.

### 2.3 절대 노출 금지

`trust`, `closeness`, `tension` 등 내부 수치와 분류명은 화면에 표시하지 않는다. 관계 상태는 문장으로만 표현한다.

## 3. 시작 화면

### 3.1 접근 제한

엔딩 이미지를 포함해 회차마다 이미지 6장이 생성되므로 무제한 공개는 비용이 새는 경로가 된다.

- 초대 코드 입력란. 코드가 맞아야 다음 단계로 진행
- 코드는 서버에서 해시 비교. 클라이언트에 정답을 두지 않는다
- 유효 코드는 `muya98`(개발자용), `tainai`(심사용) 두 개
- 코드별로 회차가 분리 저장된다
- 실패 문구는 짧게. 시도 횟수를 제한한다

### 3.2 구성

- 제목 '마지막 책갈피'
- 짧은 소개: "오늘 문을 닫는 작은 서점. 마지막 정리를 함께하며, 서로에게 남길 한 문장을 골라보세요."
- `약 10~15분` 표시
- 초대 코드 입력 → `들어가기`

코드 검증 후:

- **진행 중 회차가 있으면** `이어서 하기`를 우선 표시
- `새 이야기 시작하기` → 외형 선택 화면
- 완료된 회차가 있으면 그 아래에 회차 목록 (6절)

첫인상이 장르 이해도를 결정한다. 개발자 데모처럼 보이지 않게 하되, 과한 장식보다 여백과 타이포로 정돈한다.

## 4. 외형 선택 화면

### 4.1 목적과 제약

플레이어가 서윤의 외형을 정한다. **30초 안에 끝나야 한다.** 여기서 시간을 쓰면 본편이 짧아진다.

이름·나이·성격·말투·의상은 고정이며 선택 대상이 아니다. 화면에 "성격과 이야기는 정해져 있어요" 정도로 짧게 안내한다.

### 4.2 구성

7개 드롭다운을 2열로 배치한다. 선택지의 한글 라벨과 영문 매핑은 `CLAUDE.md` 7.2에 있다.

| 항목 | 선택지 수 |
|---|---:|
| 머리 길이 | 4 |
| 머리 색 | 4 |
| 앞머리 | 3 |
| 눈매 | 4 |
| 안경 | 3 |
| 인상 | 3 |
| 키·체형 | 3 |

**초기 상태는 전부 미선택**이다. 기본값을 넣지 않는다. 그래야 플레이어가 직접 골랐다는 감각이 생긴다.

### 4.3 주사위 버튼

드롭다운 영역 상단에 배치. 누르면 **7개 축 전부**가 각자의 선택지 중 랜덤으로 채워진다. 애니메이션 없이 즉시 반영한다.

여러 번 눌러도 되며 그 후 개별 수정도 가능하다. 조합은 5,184가지다.

### 4.4 요약과 생성

7개가 전부 선택되면 하단에 요약 문장이 나타나고 `이 모습으로 시작` 버튼이 활성화된다.

> 짙은 갈색 단발, 앞머리 없이 옆으로 넘긴 머리, 부드러운 눈매, 얇은 둥근 안경, 차분한 인상, 보통 키.

**미리보기 이미지는 생성하지 않는다.** 선택할 때마다 이미지를 만들면 비용이 감당되지 않는다. 텍스트 요약만 보여준다.

### 4.5 초상화 생성과 재생성

`이 모습으로 시작` → 초상화 1장 생성 (약 5초).

- 생성 중에는 자리를 유지한 채 스켈레톤과 `서윤을 그리는 중이에요` 표시
- 완성되면 이미지와 함께 `다시 그리기` / `이 모습으로 시작하기` 두 버튼
- **`다시 그리기`는 최대 2회.** 같은 프리셋으로 다시 뽑는다. 남은 횟수를 표시한다
- 2회를 쓰면 버튼이 비활성화되고 진행만 가능하다
- 프리셋을 바꾸고 싶으면 `외형 다시 고르기`로 4.2로 돌아간다. 이 경우 재생성 횟수는 초기화된다

`이 모습으로 시작하기` 확정 시:

1. 초상화를 서버에 저장하고 회차를 생성한다
2. **장면 이미지 4장 생성을 백그라운드로 시작한다**
3. 즉시 대화 화면으로 이동한다

장면 1 이미지는 생성 완료를 기다린 뒤 표시하고, 나머지는 대화가 진행되는 동안 준비된다. 장당 5초 내외라 장면 2가 필요한 시점(3턴 후)에는 이미 완료되어 있다.

### 4.6 주의 — 외형은 대사에 등장하지 않는다

플레이어가 고른 외형은 이미지에만 반영된다. 서윤의 대사와 서술은 외형을 언급하지 않는다. 이는 프롬프트 차원에서 막으며 UI가 별도로 처리할 것은 없다. 다만 외형 선택 화면에서 "고른 모습이 그림에 반영됩니다" 정도로 기대를 조정한다.

## 5. 대화 화면

### 5.1 좌측 — 장면 이미지

- 서윤 입상 세로 이미지 (1024×1536)
- 장면(1~4)마다 교체. 부드러운 크로스페이드
- 이미지 위에 텍스트를 얹지 않는다
- 하단에 옅은 그라데이션으로 경계를 부드럽게

**과거 스크롤 연동:** 사용자가 대화 로그를 위로 올려 이전 장면을 읽으면 좌측 이미지도 그 장면으로 바뀐다. 스크롤을 되돌리면 현재 장면으로 복귀한다. 과거를 보는 동안 이미지 위에 옅은 표식으로 `20:41 · 남겨둔 책` 정도를 표시한다.

**아직 생성 중이면** 스켈레톤과 짧은 안내를 표시한다. 대화 진행은 막지 않는다.

### 5.2 우측 상단 — 헤더

- 현재 시각과 `사이책방`
- 보조 문구 `21:00 문 닫는 시간`
- 20:30~21:00 범위의 얇은 진행 막대. 실제 초 단위 카운트다운 없음
- `대화 5/12 완료` 보조 표시. 시각보다 작게
- 우측 끝 메뉴 아이콘

### 5.3 우측 — 대화 로그

카카오톡 형태의 세로 연속 스크롤.

**말풍선 3종**

1. **서윤 대사** — 좌측 정렬. 초상화를 원형으로 자른 프로필과 이름. 연속 발화에서는 프로필 반복을 줄인다
2. **플레이어 입력** — 우측 정렬. 강조색 말풍선. 입력 원문 그대로
3. **상황 묘사** — 말풍선 없이 중앙 정렬의 이탤릭 회색 문단. 색상 차이만으로 구분하지 않는다

**장면 구분선**

```
────────  20:37 · 남겨둔 책  ────────
```

새 장면 진입 시 한 번만 추가한다. 구분선 아래에는 이미지를 넣지 않는다. 이미지는 좌측 컬럼이 담당한다.

**시스템 기록 카드**

시스템이 약속·사실·추억을 확정했을 때 대화 흐름 안에 끼워 넣는다. 얇은 테두리의 작은 카드.

```
🔖  약속이 기록되었습니다
    마지막 낭독회에서 함께 편지를 읽기로
```

| 종류 | 라벨 |
|---|---|
| 약속 | 약속이 기록되었습니다 |
| 사실 | 알게 된 사실 |
| 추억 | 기억에 남을 순간 |

이 카드는 엔딩에서 회수될 후보다. 눈에 띄되 흐름을 끊지 않을 크기로.

**등장 방식**

- 새 메시지는 아래에서 부드럽게 나타난다
- 응답 대기 중에는 타이핑 인디케이터(점 3개)
- 사용자가 위를 읽는 중이면 강제로 맨 아래로 이동하지 않고 `새 대화` 버튼을 띄운다

### 5.4 우측 하단 — 입력 영역

- **행동 힌트 칩** 2~4개를 입력창 위에 가로 배치. 누르면 입력창에 문장이 채워지고 **수정 가능**하다. 바로 전송되지 않는다
- **입력창** 여러 줄 가능. Enter 전송, Shift+Enter 줄바꿈
- **보내기 버튼** 입력이 없으면 비활성. 전송 중 중복 실행 방지
- 응답 대기 중 입력창 비활성 + 안내 문구

### 5.5 메뉴

- `저장하고 나가기`
- `여기서 이야기 마무리하기`

### 5.6 조기 종료

이 데모는 짧은 시간에 결말까지 도달하는 경험이 핵심이므로, 조기 종료를 흐린 회색으로 숨기지 않는다.

확인 모달:

> 지금까지의 이야기로 결말을 만듭니다.
> 남은 대화는 진행되지 않습니다.
> `계속 이야기하기` / `결말 보기`

확정 시 처리:

- 현재까지 확정된 사건만으로 엔딩을 생성한다. 발생하지 않은 장면의 사건을 결말이나 그림에 넣지 않는다
- 이야기 시각은 마지막 완료 턴의 시각을 유지한다. 진행 막대를 21:00까지 채우지 않는다
- 서윤의 작별 대사를 임의로 생성하지 않는다. 대화로 인사하고 떠난 경우와 구분해 기록한다
- 문 잠금은 확정되지 않은 상태로 남는다

## 6. 시각·장면·턴 규칙

이야기 시각의 권한은 서버에 있다. LLM 출력에서 시각을 추출해 헤더를 갱신하지 않는다. 표의 시각은 **해당 턴의 응답과 상태 저장이 완료된 뒤** 노출한다. 초기 상태는 20:30이다.

| 완료 턴 | 시각 | 장면 | 완료 뒤 처리 |
|---|---|---|---|
| 1 | 20:32 | 마지막 손님 | 유지 |
| 2 | 20:34 | 마지막 손님 | 유지 |
| 3 | 20:37 | 마지막 손님 | 장면 2 구분선·이미지 교체·진입 서술 |
| 4 | 20:39 | 남겨둔 책 | 유지 |
| 5 | 20:41 | 남겨둔 책 | 유지 |
| 6 | 20:45 | 남겨둔 책 | 장면 3 구분선·이미지 교체·진입 서술 |
| 7 | 20:47 | 쓰지 못한 한 문장 | 유지 |
| 8 | 20:50 | 쓰지 못한 한 문장 | 유지 |
| 9 | 20:53 | 쓰지 못한 한 문장 | 장면 4 구분선·이미지 교체·진입 서술 |
| 10 | 20:55 | 문을 닫기 전에 | 이전 기억 회수 |
| 11 | 20:58 | 문을 닫기 전에 | 마지막 입력 안내 |
| 12 | 21:00 | 문을 닫기 전에 | 마지막 응답·문 잠금 후 엔딩 |

초기 로그에는 `20:30 · 마지막 손님`을 표시한다. 장면 전환 응답은 현재 입력에 대한 반응과 다음 장면 진입 서술을 구분해 받는다. 렌더 순서는 **현재 반응 → 장면 구분선 → 좌측 이미지 교체 → 진입 서술**이다. 동일 전환을 말풍선과 구분선에서 두 번 서술하지 않는다.

읽거나 입력하는 동안 시각은 멈춘다. 응답 실패·재시도·새로고침은 턴을 소비하지 않는다. 실제 15분을 넘겨도 강제 종료하지 않는다.

## 7. 빈 카드 인터랙션

카드는 자유 대화를 엔딩의 구체적인 기억으로 연결하는 장치다. 입력창에 쓴 모든 문장을 카드 문구로 간주하지 않는다.

**턴 소비 규칙:** `이 문장으로 남기기`와 `빈 채로 두기`만 플레이어 입력 1회를 소비한다. 편집기를 열고 닫는 동작, 문구를 고쳐 쓰는 동작, 확정 전 확인 대화는 소비하지 않는다.

1. 장면 3에서 빈 카드가 등장하면 입력 영역 위에 `카드에 한 문장 남기기` 보조 동작을 제공한다. 일반 대화도 계속 가능하다
2. 편집기를 열면 빈 카드 모양의 영역과 최대 80자 입력창을 표시한다. 줄바꿈과 문장부호도 원문에 포함한다
3. `이 문장으로 남기기`를 누르면 미리보기의 문구를 그대로 확정한다
4. `빈 채로 두기`도 명시적인 행동이다. 불이익을 표시하지 않는다
5. 일반 대화에서 "이렇게 써줘"라는 의도가 있으면 문구 후보를 편집기에 채울 수 있지만, 확정 전에는 작성 완료로 기록하지 않는다

카드 작성과 일반 전송은 동일한 입력 경로를 쓴다. 동시에 두 턴을 제출할 수 없다. 카드를 쓰지 않은 채 장면 3이 끝나도 진행을 막지 않는다. 장면 4에서 아직 접근 가능하면 남은 입력으로 작성할 수 있다. 전달된 카드를 임의로 회수하거나 엔딩 뒤 문구를 수정하지 않는다.

플레이어가 작성한 문구는 **교정·번역·요약하지 않는다.** 안전한 일반 텍스트로 렌더링하며 HTML로 해석하지 않는다. 80자는 사용자에게 보이는 문자 단위로 클라이언트와 서버 기준을 맞춘다. 공백만 있는 입력은 확정할 수 없다.

## 8. 엔딩 화면

**좌우 2단을 유지하되 대화 화면과 시각적으로 구분한다.** 배경 톤을 한 단계 어둡게 하거나 여백을 넓혀, 이야기가 끝났다는 감각을 준다.

- **좌측** — 생성된 엔딩 이미지 (세로 1024×1536). 대화 중의 장면 이미지와 같은 자리에 놓여 마지막 그림이라는 것이 자연스럽게 전달된다
- **우측** — 아래 순서로 세로 스크롤

### 8.1 우측 구성

1. **엔딩 제목** — 큰 타이포, 충분한 여백
2. **결말 본문** — 3~5문단. 소설 본문처럼 조판. 읽기 좋은 행간
3. **카드 문장** — `당신이 카드에 남긴 문장` 아래 원문 표시. 서윤이 쓴 문장이면 작성자를 다르게 표시. 빈 카드면 `카드는 빈 채로 남았다`처럼 사실대로 표시하고 인용문을 만들어내지 않는다
4. **이 결말에 남은 대화** ← 가장 중요
5. **아직 남은 것** — 미해결로 남은 문제를 한두 줄. 작고 조용한 스타일
6. **하단 동작** — `대화 다시 보기` / `다른 회차 보기` / `새 이야기 시작하기`

### 8.2 `이 결말에 남은 대화` — 최우선 완성도

**이 프로젝트에서 가장 중요한 화면 요소다.** 플레이어가 남긴 말이 결말의 근거로 회수되는 것을 보여준다.

2~3개 항목을 카드로 표시한다.

```
┌──────────────────────────────────┐
│  20:41 · 남겨둔 책                │
│                                  │
│  "책갈피, 계속 쓰고 있어요."       │
│                                  │
│  ────────────────────────        │
│  이 말이 마지막 부탁의 바탕이 되었다. │
└──────────────────────────────────┘
```

- 상단에 시각과 장면 이름
- 중앙에 **플레이어가 실제로 입력한 원문**을 인용 형태로
- 하단에 그것이 결말에 어떻게 작용했는지 한 문장

카드는 스크롤에 맞춰 **하나씩 순차적으로** 나타난다. 한꺼번에 뜨지 않는다. 이 순차 등장이 "내가 한 말이 여기 남았구나"를 만든다.

누르면 대화 로그의 해당 턴으로 이동한다. 인용은 메시지 ID와 원문을 연결해 표시하며, LLM이 다시 쓴 문장을 쓰지 않는다.

조기 종료로 대화가 적으면 존재하는 근거만 사용한다. 억지로 개수를 채우지 않는다.

### 8.3 마무리 문구

`새 이야기 시작하기` 옆에 짧게 덧붙인다.

> 같은 시작에서도 다른 결말이 나옵니다.

두 번째 플레이를 유도하는 장치다. 서로 다른 회차를 비교하는 것이 이 데모의 증명 방식이다.

## 9. 회차 목록

코드별로 회차를 서버에 저장한다. 시작 화면과 엔딩 화면 양쪽에서 접근한다.

### 9.1 목록 항목

| 요소 | 내용 |
|---|---|
| 썸네일 | 그 회차의 초상화를 원형으로 자른 이미지 |
| 회차 번호 | `3번째 이야기` |
| 엔딩 제목 | 완료된 회차만. 미완료는 `진행 중 (7/12)` |
| 날짜 | 플레이한 날짜 |

**썸네일이 중요하다.** 회차마다 외형이 다르므로 썸네일만 봐도 구분된다. 여러 회차를 비교하는 경험이 이 데모의 핵심이므로 목록이 밋밋하지 않아야 한다.

### 9.2 동작

- 완료된 회차를 누르면 **읽기 전용**으로 엔딩 화면을 연다. 거기서 대화 로그와 이미지 6장을 모두 볼 수 있다
- 미완료 회차는 `이어서 하기`로 진행 가능
- 읽기 전용 상태에서는 입력창과 메뉴의 종료 항목을 숨긴다
- 과거 회차를 편집하거나 엔딩을 다시 생성하지 않는다

## 10. 상태와 실패 처리

| 상황 | 화면 동작 |
|---|---|
| 초상화 생성 중 | 스켈레톤, `서윤을 그리는 중이에요` |
| 초상화 생성 실패 | 같은 프리셋으로 재시도 버튼. 재생성 횟수를 소모하지 않는다 |
| 장면 이미지 미완성 | 좌측에 스켈레톤. 대화는 정상 진행 |
| 대화 응답 대기 | 입력 원문 유지, 타이핑 인디케이터, 중복 전송 차단 |
| 대화 실패 | 같은 요청 재시도. 완료 턴과 시각은 그대로 |
| 엔딩 본문 생성 중 | 마지막 대화 유지, `마지막 장면을 정리하고 있어요` |
| 엔딩 이미지 생성 중 | 본문·카드 먼저 표시, 좌측 자리의 상태만 갱신 |
| 이미지 생성 30초 초과 | `그림을 만드는 중이에요. 완성되면 이 회차에서 확인할 수 있어요` |
| 이미지 생성 실패·거부 | 본문 보존, `그림 다시 만들기` 제공. 중복 생성 방지 |
| 화면 재진입 | 저장된 회차와 이미지 작업 상태 복원 |

30초는 문구 전환 기준이며 완료 약속이 아니다. 일시적인 화면 이탈은 이야기 속 퇴장으로 처리하지 않는다.

실패 화면이 요란하지 않게 한다. 이 게임은 잔잔한 톤이 전부다.

## 11. 화면 제작 시 참고

이 화면들은 채용 포트폴리오로 제출된다. 심사자는 AI 캐릭터 채팅 서비스를 만드는 팀이다. 다음이 화면에서 읽혀야 한다.

1. **캐릭터를 내가 만들었다** — 외형 선택과 생성된 초상화
2. **유한한 시간과 완결감** — 20:30에서 21:00으로 다가가는 시계, 장면 구분선
3. **대화가 구조화되고 있다** — 시스템 기록 카드, 카드 컴포넌트
4. **선택이 결말에 남는다** — 엔딩의 `이 결말에 남은 대화`
5. **로맨스 장르에 대한 이해** — 전반적인 톤과 조판

기능 나열보다 위 다섯이 전달되는 것이 우선이다.

**데모 데이터:** 서버 연동 전에 화면을 만들 때는 12턴 분량의 대화, 장면 구분선 4개, 시스템 기록 카드 3개, 카드 문장 1개, 엔딩 근거 대화 3개, 이미지 6장 자리를 채워 넣는다. 빈 화면으로는 완성도를 판단할 수 없다.

## 12. 검수 항목

- 날짜·일차·슬롯 점이 남아 있지 않고, 모든 시각이 `HH:mm`으로 표시되는가
- 외형 7개를 선택해야 생성 버튼이 활성화되는가. 주사위가 7개를 모두 채우는가
- 재생성이 2회로 제한되고 남은 횟수가 보이는가
- 좌측 이미지가 장면 전환에 맞춰 교체되고, 과거 스크롤 시 함께 바뀌는가
- 3·6·9턴 완료 시 구분선이 한 번만 생기며, 재시도·새로고침에도 순서와 시각이 유지되는가
- 읽는 중 또는 입력 중에는 시간이 흐르지 않는가
- 관계 수치나 내부 분류명이 어디에도 노출되지 않는가
- 카드의 띄어쓰기·줄바꿈·문장부호·이모지가 엔딩까지 정확히 유지되는가
- 빈 카드·선물 거절·약속 미수락·먼저 떠남이 엔딩 텍스트와 그림에 반영되는가
- 메뉴 조기 종료가 대화 작별과 구분되고, 진행 막대가 21:00으로 채워지지 않는가
- 마지막 입력 전에 안내가 보이고, 마지막 응답이 새 질문 없이 끝나는가
- 이미지 지연·실패에도 본문과 카드, 대화 기록을 읽을 수 있는가
- 초대 코드 없이 새 회차를 시작할 수 없는가
- 회차 목록의 썸네일이 회차마다 다르게 보이는가
- 과거 회차가 읽기 전용으로 열리는가

## 13. 플레이테스트

첫 탐색 테스트는 5~8명으로 시작한다. 표본이 작으므로 통계적 우열을 주장하지 않는다.

기록할 것: 실제 플레이 시간, 외형 선택에 쓴 시간, 장면 4 체류 시간, 마지막에 남은 말의 내용, 종료가 급했는지.

마지막 장면에서 표현을 마치지 못한 경험이 반복되면 장면 4만 4턴으로 늘리는 구성(총 13턴)을 비교한다. 1~9턴은 같고 10~13턴 완료 시각을 20:55, 20:57, 20:59, 21:00으로 둔다. 회차 시작 시 구성을 고정하며 진행 중 확장은 없다.
