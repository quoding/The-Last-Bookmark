# 2026-09-07 작업 기록 (Claude) — 프론트 통합 대조 반영

## 배경
Codex의 프론트엔드 1차 구현 완료 보고(`worklog_codex/26092105_worklog.md`, `frontend/INTEGRATION.md`)를 검토하고,
실제 백엔드 코드와 대조해 확인한 문제를 고쳤다. 이어서 사용자가 요청한 계약 확장(코드별 저장 한도,
재진입 상태 노출, 코드별 API 키, 비용 모니터링)을 구현했다.

## 1. 즉시 수정 (버그, 계약 변경 아님)
- **CORS 미들웨어 부재**: 실제 브라우저에서 프론트↔백엔드 연결이 막혀 있었다. `CORS_ALLOWED_ORIGINS` 환경변수와
  `CORSMiddleware`를 추가했다.
- **오프닝 대사(turn=0) 미저장**: story.md 6.1의 첫 화면 설명·시작 대사가 어디에도 저장되지 않아 다른
  브라우저에서 재진입 시 복원이 불가능했다. `engine/opening.py`에 고정 텍스트로 옮기고 `/start` 호출 시
  turn=0 메시지로 한 번만 기록하게 했다.
- **테스트 하네스 버그**: `app.models`를 임포트하지 않은 채 단일 테스트 파일만 실행하면 `Base.metadata`에
  테이블이 등록되지 않아 `create_all`이 아무것도 만들지 않는 문제. `tests/conftest.py`에 `app.models` 임포트를
  명시했다.

## 2. Codex 대조 항목 중 "변경 불필요"로 확인된 것
- 카드 문장은 엔딩 근거 목록이 아니라 `EndingResponse.card`(별도 섹션, ui-spec 8.1의 3번 항목)에 원문 그대로
  들어간다. player 메시지 ID로 연결할 필요 없음.
- 장면 이미지 실패/재시도 경로 부재는 API 계약(CLAUDE.md 6장)에 의도된 설계다. ui-spec도 스켈레톤 표시로
  충분하다고 명시함.
- 장면 전환 시 "반응/진입 서술 구분"은 LLM 출력이 `reply`+`narration` 한 세트뿐이라(CLAUDE.md 8.1) 별도 필드가
  없다. 전환 턴에도 하나의 응답 안에 반응과 다음 장면 연결을 함께 쓰는 게 설계 의도(CLAUDE.md 7.2).

## 3. 사용자 요청으로 추가된 계약 확장
사용자와 직접 확인 후 진행:

- **회차 저장 한도를 코드별로 변경**: `IMAGE_BUDGET_SESSIONS`가 전체 코드 합산으로 세고 있던 버그를 고쳐
  코드 1개당 한도로 바꿨다. 기본값도 100 → 3으로 낮춤 (코드당 3가지 커스터마이징으로 3가지 엔딩을 보고
  싶다는 요청).
- **재진입 상태 복원 확장**: `GET /api/sessions/{id}`에 다음을 추가.
  - `portrait_confirmed`(bool), `presets`: 초상화만 만들고 "이 모습으로 시작하기" 전에 이탈한 회차인지
    구분해 프론트가 올바른 화면(초상화 확인/대화)으로 보낼 수 있게 함.
  - `scenes`: 4장면 이미지 전체 배열. 완료 회차를 다른 브라우저에서 열어도 장면 4장을 전부 복원 가능.
- **`POST /sessions`, `POST /portrait/retry`에 `request_id` 멱등 처리 추가**: 응답 유실 시 재시도해도
  중복 생성되지 않는다. (계약 변경 — 두 엔드포인트 모두 body에 `request_id`가 이제 필수)
- **코드별 API 키 분리**: `CODE_API_KEYS` 환경변수("code_id:api_key,code_id:api_key")로 초대 코드마다 다른
  LLM/이미지 API 키를 쓸 수 있게 함. muya98(개발/테스트용)과 tainai(제공용) 비용을 분리 추적하려는 목적.
  매핑에 없는 코드는 기존 전역 `LLM_API_KEY`/`IMAGE_API_KEY`로 폴백. `app/api/deps.py`, `app/api/deps_llm.py`에서
  코드별로 클라이언트 인스턴스를 캐시하고, 백그라운드 이미지 작업(`_run_background_scene_jobs`,
  `_run_ending_image_job`)도 세션의 code_id를 조회해 올바른 키를 쓰도록 수정.
- **API 비용 모니터링 페이지**: `api_usage_logs` 테이블에 LLM·이미지 호출마다 실제 토큰 사용량(text/image/output
  tokens)을 코드별로 기록. `/admin`(HTML 페이지) + `/api/admin/usage`(집계 API)를 `ADMIN_PASSWORD` 환경변수로
  보호해 추가. 비용(₩/$) 계산은 서버에 저장하지 않고 페이지에서 조회 시점에 단가를 입력해 계산한다
  (LLM_MODEL인 gpt-5.6-luna의 실제 단가가 아직 불확실하고, 이미지는 `docs/index.html`의 gpt-image-2 공식
  단가를 기본값으로 재사용). 이 두 라우트는 CLAUDE.md 6장 API 계약(프론트 접점)에는 포함되지 않는
  별도 관리자 도구다.

## 커밋
- `c5844ce`: CORS·오프닝 대사 수정
- `ed8fb53`: 코드별 API 키·회차 한도, 재진입 상태 노출, 비용 모니터링 추가

## 테스트
전체 98개 통과 (기존 87개 + 신규 11개: 오프닝/CORS 3개, 계약 확장 7개, 관리자 페이지 4개 — 일부 중복 제외).
실제 postgres 컨테이너 대상, LLM·이미지 호출은 목으로 대체.

## 프론트(Codex)에 전달해야 할 계약 변경
1. `POST /api/sessions`, `POST /api/sessions/{id}/portrait/retry` 요청 본문에 `request_id`(string) 필수 추가.
2. `GET /api/sessions/{id}` 응답에 `portrait_confirmed`(bool), `presets`(PresetSelection), `scenes`(4개짜리
   `{id, name, image_url}` 배열) 필드 추가. 재진입 라우팅과 완료 회차 과거 장면 이미지 복원에 사용.
3. `/admin`, `/api/admin/usage`는 게임 화면과 무관한 별도 관리자 도구이므로 프론트 구현 대상이 아님.

## 남은 검증 항목
- Docker 이미지 빌드는 확인했으나 실제 docker-compose로 postgres+api를 함께 띄워보는 종단 테스트는 아직 안 함.
- 카드 80자 기준(그래프클러스터 vs 코드포인트) 불일치는 그대로 둠 — 새 의존성 없이 정확히 맞추기 어려워
  사용자에게 별도 보고, 아직 결정 없음.

---

## 22:31 — Codex 전달 사항 정리

사용자가 위 계약 변경을 Codex에게 직접 전달하기로 함. 전달한 요약:

**1. 계약 변경 (필수 반영)**
- `POST /api/sessions`, `POST /api/sessions/{id}/portrait/retry` 요청 본문에 `request_id`(string) 필수.
- `GET /api/sessions/{id}` 응답에 `portrait_confirmed`, `presets`, `scenes`(4개 `{id, name, image_url}`) 추가.

**2. 이미 처리되어 프론트가 손댈 필요 없는 것**
- CORS, 오프닝 대사(turn=0) 서버 저장.
- "카드 원문↔player 메시지 연결" 이슈는 오해였음 — 카드는 `EndingResponse.card` 별도 섹션에 원문 그대로 들어감.

**3. 프론트 구현 대상 아님**: `/admin`, `/api/admin/usage`.

**4. 미해결**: 카드 80자 기준 grapheme vs 코드포인트 차이, 결정 보류.

**주의**: 사용자가 "Codex 작업 중이니 push·commit 하지 말라"고 지시함. 이번 세션의
worklog 갱신은 로컬 파일 수정만 하고 git commit/push는 보류한다. Codex의 프론트 작업이
끝난 뒤 한 번에 커밋·푸시할 것.

---

## 23:54 — 실제 API 키로 종단 검증, 회차 삭제 기능, 환경 관리 정리

### 실제 백엔드+프론트 연동 검증 (사용자 요청)

- postgres(docker-compose) + uvicorn 백엔드(로컬) + vite 프론트를 실제로 띄워 mock 없이 연결 확인.
- 인증, CORS, 회차 생성/목록/재진입 라우팅(`portrait_confirmed`/`presets`/`scenes` 필드 실제 동작), 초상화 재시도,
  request_id 멱등성, 코드당 회차 한도(403) 전부 Playwright로 브라우저 단에서 직접 확인.
- 사용자가 실제 OpenAI API 키를 `backend/.env`(`CODE_API_KEYS`, 코드별 분리)에 입력한 뒤 재기동해
  **진짜 이미지 생성·LLM 대화·엔딩까지 12턴 전체를 실제 API로 완주**:
  - 초상화 → 장면1~4 → 엔딩까지 캐릭터 일관성 유지 확인(이미지 직접 열어봄).
  - 장면 전환(3·6·9턴), 외형 미언급, 소품 묘사, book_given 등 confirmed_events, 카드 작성,
    엔딩 근거 원문 그대로 인용, 미해결 항목까지 스펙대로 동작.
  - 이미지 6장(초상화1+장면4+엔딩1)·LLM 13회(턴12+엔딩1) 호출 수 정확히 일치, 실제 비용 약 $0.1.
- **발견한 실제 버그**: `openai==1.57.4`가 `gpt-image-2`의 `quality='low'`, `output_format='webp'`를
  지원 안 해 초상화 생성이 전부 실패하고 있었음. `openai==1.99.9`로 업그레이드해 해결(커밋 `63028c2`).
  테스트 98개 그대로 통과 확인.
- 부수적으로 발견: `backend/.env`가 로컬에 실제로 존재하면 pytest가 그 값(CORS 등)을 읽어 흔들리는
  테스트 하나 발견 → conftest에서 관련 env를 테스트 기본값으로 고정(같은 커밋에 포함).

### 회차 삭제 기능 (사용자 요청)

- `DELETE /api/sessions/{id}` 추가. `app/engine/session_admin.py`에서 sessions<->images 순환 참조를
  안전하게 끊고 이미지 파일까지 실제로 지움. `api_usage_logs`는 비용 집계용이라 남기고 `session_id`만 비움.
  진행 중/완료 상관없이 코드 소유자만 삭제 가능. 테스트 6개 추가, 전체 104개 통과(커밋 `3f8c552`).
- Codex가 프론트 반영(삭제 버튼·확인 모달·404/실패 처리, 커밋 `5edac4f`) — 실제 백엔드로 브라우저에서
  삭제 확인 모달 → DELETE 호출(200) → 목록 갱신 → 서버 DB 반영까지 직접 검증.

### 환경 관리 실수와 정리

- 실제 백엔드 검증용으로 임시로 만든 `frontend/.env.preview.local`을 정리하다가 실수로 지워서,
  사용자가 재플레이했을 때 mock 더미 이미지가 나오는 문제가 생김(포트 5175가 조용히 mock으로 돌아감).
- **재발 방지로 방식을 바꿈**: 앞으로 실제 백엔드 검증용 프론트(포트 5175)는 `.env` 파일을 전혀 쓰지 않고,
  `VITE_API_BASE_URL`을 서버 실행 커맨드에 인라인 환경변수로 직접 넘겨서 띄운다. Codex의 `frontend/.env.local`
  (포트 5173, mock 고정)은 앞으로 절대 건드리지 않는다. 두 서버가 파일을 공유하지 않으므로 서로 덮어쓸 일이 없음.
- 사용자에게 `backend/.env`에 API 키 입력 위치 안내(`LLM_API_KEY`/`IMAGE_API_KEY` 전역, 또는
  `CODE_API_KEYS`로 코드별 분리) — 실제로는 코드별 분리를 택해 `CODE_API_KEYS`에 muya98/tainai 각각의
  실제 키를 입력받음(값 자체는 대화에도 로그에도 남기지 않음).

### 현재 실행 중인 것 (이 세션 종료 시점)
- `tainai-db-1`(docker, postgres): 실행 중
- 백엔드(uvicorn, 포트 8010, `backend/.env` 사용): 실행 중, 실제 API 키로 동작
- 프론트 mock(vite, 포트 5173): Codex가 띄워둔 것, 그대로 둠
- 프론트 real(vite, 포트 5175): 인라인 env로 실제 백엔드 연결, 실행 중
- `/admin` 비용 모니터링: `ADMIN_PASSWORD=preview1234`로 활성화됨(`backend/.env`)

---

## 00:39 — 사용자 플레이 피드백 6가지 처리, 회차 삭제 실사용 중 발견한 두 버그 수정

### 사용자 피드백 분류
1. 첫 장면 대기시간이 김 — 조사 결과 설계는 이미 요청대로였다(장면1만 동기, 2~4는 백그라운드).
   다만 실측 gpt-image-2 응답이 CLAUDE.md가 가정한 5초가 아니라 20~30초라 체감 대기가 길다.
   초상화+장면1 두 번의 동기 대기가 합쳐져 최초 진입까지 40~60초 걸림. 아키텍처 변경(장면1도
   비동기로 돌릴지)은 스펙 변경이라 사용자에게 확인 필요 — 아직 미결.
2·4·6. 이미지 로딩 중 순차 문구, 타이핑 인디케이터 스크롤 버그, 완료 회차 스크롤 힌트는 전부
   프론트 UX라 Codex에게 전달하도록 안내.
3. 플레이어 메시지가 답장과 함께 늦게 뜨는 문제도 프론트(optimistic UI 부재)라 Codex 전달 안내.
5. 서윤이 종결형으로만 답해 플레이어가 막막해지는 문제는 LLM 프롬프트 이슈라 직접 수정.
   `app/llm/prompts.py`에 `GUIDANCE_NOTES` 추가(가끔 다음 행동을 자연스럽게 열어두기, 고정
   사건에 서윤이 먼저 다가가기 등). 실제 API로 재검증: 애매한 입력("그렇군요")에도 서윤이
   구체적인 다음 행동을 제안하는 것 확인.

### 회차 삭제 실사용 중 발견한 버그 2건 (둘 다 실제 API로 재현·수정·재검증 완료)

**버그 A — 삭제가 배경 이미지 작업과 경합해 500 에러.** 장면 2~4 이미지는 `/start` 이후
BackgroundTasks로 순차 생성되는데(장당 20~30초), 그 도중 회차를 삭제하면 백그라운드 작업이
새 이미지 행을 커밋해 `DELETE FROM sessions`가 FK 위반으로 실패했다. 처음엔 Python에서 재시도
(`time.sleep` 후 재삭제)로 고쳤다고 봤으나, 실측 이미지 생성 시간(20~30초/장)이 재시도 창보다
훨씬 길어 여전히 실패 재현됨. **근본 원인은 Python 레벨 순서 보장으로는 이 경합을 이길 수
없다는 것.** session_id를 참조하는 모든 FK에 `ON DELETE CASCADE`(비용 로그는 `SET NULL`)를
거는 마이그레이션(`194bd16e37e3`)으로 교체해, `DELETE FROM sessions` 한 문장을 DB가 원자적으로
처리하게 했다. `app/engine/session_admin.py`가 훨씬 단순해짐(수동 자식 삭제 코드 전부 제거).
배경 작업 쪽도 세션이 이미 지워진 뒤 실행되면 조용히 멈추도록 방어 코드 추가
(`_run_background_scene_jobs`, `_run_ending_image_job`). 실제 API로 "생성 직후 바로 삭제"를
재현해 200 응답과 DB 완전 정리 확인, 백그라운드 로그에 에러도 안 남는 것까지 확인.

**버그 B — 회차 삭제 후 새 회차 생성 시 index 충돌(UniqueViolation).** `next_index`를
`count(기존 회차)+1`로 계산했는데, 회차를 삭제할 수 있게 되면서 count가 실제 최대 번호를
보장 못 하게 됨(예: 남은 회차가 {2,3,4}면 count=3, count+1=4가 이미 존재). `MAX(index)+1`로
교체. 실제 서버에서 이 충돌로 회차 생성이 500 나는 것 직접 재현 후 수정 확인.

### 테스트
- 신규 3개(회차 삭제 경합 재현, index 충돌 재현) 포함 전체 106개 통과.
- 마이그레이션 `194bd16e37e3` up/down/up 왕복 및 `alembic check`로 모델과 일치 확인.
